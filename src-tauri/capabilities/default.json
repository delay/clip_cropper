{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Default capability for the clip cropper window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
